# vorm (TypeScript / JavaScript)

Planned. Same product model as the Go implementation:

- Declarative schema and migrations
- `vorm:query` stubs lowered to parameterized SQL with typed clients

Not implemented yet — use the Go implementation at the [repository
root](../README.md).
